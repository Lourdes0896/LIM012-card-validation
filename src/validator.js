const validator = {
  // ...
};

export default validator;

const validador=(numeroTarjeta)=>{

  for(let i=0;i<numeroTarjeta.len)
  //let reverso =numeroTarjeta.reverse().join('');
  //console.log(numero);
}